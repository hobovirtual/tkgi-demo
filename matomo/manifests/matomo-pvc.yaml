apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: matomo-pv-claim
spec:
  storageClassName: default-storageclass
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi